package gprofiler.internal.ui;

import javax.swing.*;
import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import java.awt.event.ItemEvent;
import java.awt.event.ItemListener;

/**
 * @description A small panel in the settingsPanel which decides where to store the output of the
 * gProfiler test on the user's system. The user has access to the check box which they will use
 * to enable the SaveResults option and a textfield where they fill the path and name of the file
 * that needs to be saved.
 */
public class SaveResultsPanel extends JPanel implements ItemListener, ActionListener {
    @Override
    public void actionPerformed(ActionEvent e) {

    }

    @Override
    public void itemStateChanged(ItemEvent e) {

    }
    /**
     *
     */
}
